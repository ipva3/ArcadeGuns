{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "Build project",
      "command": "${workspaceFolder}/build.bat",
      "args": [
        "debug"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "windows": {
        "problemMatcher": "$msCompile"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}